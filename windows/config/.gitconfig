# Identity and shared defaults come from common/config/.gitconfig, linked to
# ~/.gitconfig.common by setup.ps1. Only Windows-specific settings live here.

[include]
	path = ~/.gitconfig.common

[credential]
	helper = manager

[core]
	autocrlf = false
	longpaths = true
	editor = vim
