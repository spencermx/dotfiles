# Keep PulseAudio in charge of audio and Bluetooth. WirePlumber supplies the
# policy/linking required for PipeWire screen sharing (like video-only profile).
wireplumber.profiles = {
    main = {
        hardware.audio = disabled
        hardware.bluetooth = disabled
    }
}
